{
  "name": "@ai-brainstorm/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "prisma generate",
    "build": "prisma generate",
    "migrate:create": "wrangler d1 migrations create ai_brainstorm",
    "migrate:apply:local": "wrangler d1 migrations apply ai_brainstorm --local",
    "migrate:apply:remote": "wrangler d1 migrations apply ai_brainstorm --remote",
    "generate:types": "wrangler types"
  },
  "dependencies": {
    "@prisma/adapter-d1": "^5.14.0",
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250416.0",
    "prisma": "^5.14.0"
  }
}